<%= render partial: 'teams', locals: { teams: @teams } %>
<%= render partial: 'users', locals: { users: @users } %>
<%= render partial: 'matches', locals: { most_recent_successful_matches: @most_recent_successful_matches, most_recent_unsuccessful_matches: @most_recent_unsuccessful_matches } %>
